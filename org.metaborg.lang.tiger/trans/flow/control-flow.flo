module flow/control-flow

flow rules

Seq(exprs): seq exprs

If(cond, then, else): cond, (then || else)

IfThen(cond, then): cond, (then || end)

While(cond, body): cond, (body, jump cond || end)

For(_, from, to, body): (from || to), body

b@Break: l
  where b |-> l // .parentStatement.next
